# ── Local environment ────────────────────────────────────────────────────────
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# ── Build artefacts ──────────────────────────────────────────────────────────
# Lambda zips produced by infra/deploy.sh
infra/.build/
# Dockerfile generated by `agentcore configure`
.bedrock_agentcore/

# ── Starter code ─────────────────────────────────────────────────────────────
# Pristine copy of github.com/udacity/cd14763-project-starter, kept on disk as a
# diff baseline. Not part of the submission — the working files live at the
# project root.
starter/
